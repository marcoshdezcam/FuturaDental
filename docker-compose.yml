services:
  frontend:
    image: wordpress
    container_name: frontend
    ports:
      - "8088:80"
    restart: always
    depends_on:
      - frontend_db
    environment:
      WORDPRESS_DB_HOST: frontend_db:3306
      WORDPRESS_DB_USER: marcos
      WORDPRESS_DB_PASSWORD: Nacimi627267
      WORDPRESS_DB_NAME: frontend
    volumes:
      - frontend:/var/www/html

  frontend_db:
    image: mysql
    container_name: frontend_db
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - "3306:3306"
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Nacimi627267
      MYSQL_DATABASE: frontend
      MYSQL_USER: marcos
      MYSQL_PASSWORD: Nacimi627267
    volumes:
      - frontend_db:/var/lib/mysql

  admin_db:
    image: postgres
    container_name: admin_db
    ports:
      - "5432:5432"
    restart: always
    environment:
      POSTGRES_USER: marcos
      POSTGRES_PASSWORD: Nacimi627267
      POSTGRES_DB: admin
    volumes:
      - admin_db:/var/lib/postgresql/data

  admin:
    build:
      dockerfile: ./admin/Dockerfile
    restart: always
    container_name: admin
    working_dir: /app
    volumes:
      - admin:/app
    networks:
      - admin
    ports:
      - "3000:3000"
    expose:
      - "3000"

  api:
    build:
      dockerfile: ./api/Dockerfile
    restart: always
    container_name: api
    working_dir: /app
    volumes:
      - api:/app
    networks:
      - admin
    ports:
      - "2222:2222"
    depends_on:
      - admin_db
    environment:
      - NODE_ENV=development
    expose:
      - "2222"

volumes:
  frontend:
  frontend_db:
  admin_db:
  admin:
  api:

networks:
  admin:
    driver: bridge
